{{! Import the contents of header.html from partials}}
{{> navigation}}

    <div class="row">
        <div class="col-md-4"></div>
        <div class="col-md-4">
            {{#if error}}
             <div class="form-group row">
                <div class="col-10">
                    <div class="alert alert-danger">{{error}}</div>
                </div>
            </div>
             <div class="form-group row">
                <div class="col-10">
                    <a href="/policiesSearcher" class="btn btn-default">Go Searcher</a>
                </div>
            </div>
            {{/if}}
        </div>
        <div class="col-md-4"></div>
    </div>